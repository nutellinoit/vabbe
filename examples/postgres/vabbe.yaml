name: ex-postgres
network: { subnet: 10.152.1.0/24 }
defaults: { image: ${VABBE_NODE_IMAGE}, privileged: true }
nodes:
  - { name: pg-primary, ip: 10.152.1.10 }
  - { name: pg-replica, ip: 10.152.1.11 }
